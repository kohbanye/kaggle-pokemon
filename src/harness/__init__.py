"""Evaluation harness: pure result attribution and aggregation.

The engine-touching match driver lives in ``scripts/run_eval.py`` (Docker only);
everything here is pure Python so it type-checks and unit-tests natively.
"""
